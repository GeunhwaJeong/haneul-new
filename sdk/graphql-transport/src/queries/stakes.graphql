# Copyright (c) Mysten Labs, Inc.
# SPDX-License-Identifier: Apache-2.0

query getStakes($owner: HaneulAddress!, $limit: Int, $cursor: String) {
	address(address: $owner) {
		stakedHaneuls(first: $limit, after: $cursor) {
			pageInfo {
				hasNextPage
				endCursor
			}
			nodes {
				...RPC_STAKE_FIELDS
			}
		}
	}
}

query getStakesByIds($ids: [HaneulAddress!]!, $limit: Int, $cursor: String) {
	objects(first: $limit, after: $cursor, filter: { objectIds: $ids }) {
		pageInfo {
			hasNextPage
			endCursor
		}
		nodes {
			asMoveObject {
				asStakedHaneul {
					...RPC_STAKE_FIELDS
				}
			}
		}
	}
}

fragment RPC_STAKE_FIELDS on StakedHaneul {
	principal
	activatedEpoch {
		epochId
		referenceGasPrice
	}
	stakeStatus
	requestedEpoch {
		epochId
	}
	activatedEpoch {
		epochId
	}
	# staking pool can be read from contents
	contents {
		json
	}

	address
	estimatedReward
	# validatorAddress?
}
