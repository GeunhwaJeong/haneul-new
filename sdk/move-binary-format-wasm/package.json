{
  "name": "move-binary-format-tests",
  "version": "0.1.0",
  "description": "Move Binary Format implementation in WASM",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target nodejs --scope haneullabs",
    "build:dev": "wasm-pack build --target nodejs --debug --scope haneullabs",
    "build:release": "wasm-pack build --target web --release --scope haneullabs",
    "test": "vitest run"
  },
  "author": "Haneul Labs <build@haneul-labs.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "tsup": "^7.1.0",
    "typescript": "^5.1.6",
    "vitest": "^0.33.0",
    "wasm-pack": "^0.12.1"
  }
}
