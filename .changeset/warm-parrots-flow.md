---
'@haneullabs/dapp-kit': patch
---

Add `getHaneulClientQuery` to get the `queryOptions` config for usage with the `QueryClient` outside of React hooks. Added `useHaneulClientSuspenseQuery` to support suspense-based data fetching.
