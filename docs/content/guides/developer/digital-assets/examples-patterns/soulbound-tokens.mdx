---
title: Soulbound NFTs
description: Use Haneul Move struct abilities and the Haneul framework transfer module to make an NFT soulbound (non-transferable).
keywords: [ERC-721, NFT, soulbound]
draft: true
---

A soulbound non-fungible token (NFT) is an NFT that is non-transferable. After an NFT is minted to a Haneul account, the NFT is bound to that account and cannot be transferred. This implementation uses the custom logic of the Haneul framework `transfer` functions. The <UnsafeLink href="/references/framework/haneul-framework/haneul_haneul/transfer">`haneul::transfer` module</UnsafeLink> contains 2 functions that transfer objects: <UnsafeLink href="/references/framework/haneul-framework/haneul_haneul/transfer#function-transfer">`transfer::transfer`</UnsafeLink> and <UnsafeLink href="/references/framework/haneul-framework/haneul_haneul/transfer#function-public_transfer">`transfer::public_transfer`</UnsafeLink>.

Typically, when you define new NFTs or object types on Haneul, you do not need to create a transfer function because the Haneul framework offers `transfer::public_transfer`, which anyone can use to transfer objects. `transfer::public_transfer` requires that transferred objects have the `key` and `store` abilities. Therefore, if you define a new NFT type that has the `key` ability (meaning it is a Haneul object) but not the `store` ability, holders cannot use `transfer::public_transfer`. This results in a soulbound NFT.

You can also create custom transfer logic for NFTs on Haneul. The `transfer::transfer` function has custom rules performed by the Haneul Move bytecode verifier that ensure the transferred objects are defined in the module where transfer is invoked. While removing the `store` ability from a struct definition makes `transfer::public_transfer` unusable, you can still use `transfer::transfer` as long as you invoke it in the module that defined that object type. This allows the module owner to provide custom transfer logic for soulbound NFTs.

The following example creates a soulbound NFT on Haneul. The `TestnetSoulboundNFT` struct defines the NFT with `id`, `name`, `description`, and `url` fields:

<ImportContent source="examples/move/nft-soulbound/sources/testnet_soulbound_nft.move" mode="code" struct="TestnetSoulboundNFT" noComments />

The `TestnetSoulboundNFT` struct has the `key` ability but not the `store` ability. This means you cannot transfer it with `transfer::public_transfer`. Instead, use `transfer::transfer` with custom transfer logic implemented in the same module.

This example also shows how to provide custom transfer logic using the `transfer::transfer` function. This is where you can add additional logic, such as resetting the NFT stats or requiring a payment. Do not provide this functionality if the NFT is fully soulbound:

<ImportContent source="examples/move/nft-soulbound/sources/testnet_soulbound_nft.move" mode="code" fun="transfer" />

[View the full example on GitHub](https://github.com/GeunhwaJeong/haneul/tree/main/examples/move/nft-soulbound/sources/testnet_soulbound_nft.move).