---
title: Configure a Haneul Client
description: The Haneul client configuration specifies which network to connect to and which address to send transactions.
keywords: [ haneul client, haneul configuration, client.yaml, client config, local config, configure tooling, configure haneul client, configure haneul, config haneul ]
pagination_prev: null
pagination_next: null
---

The Haneul client configuration specifies which network to connect to and which address to send transactions.

<Tabs className="tabsHeadingCentered--small">
<TabItem value="prereq" label="Prerequisites">

First, confirm that Haneul has been installed successfully:

```
$ haneul --version
```

If this command returns `haneul not found`, then Haneul is not installed and you must [follow the installation instructions](/guides/developer/getting-started/haneul-install.mdx).

</TabItem>
</Tabs>

## `haneul client`

Run the Haneul CLI with the command:

```
$ haneul client
```

:::info

If a previous Haneul installation stored a `client.yaml` file locally, you will receive the `haneul client --help` output in the console. You can delete the existing `~/.haneul/haneul_config/client.yaml` file if you'd like to start fresh, or you can continue using the existing configuration.

:::

The prompt asks if you want to create the `client.yaml` file, select `Y` or press enter. You can skip the prompt with `haneul client -y`.

```
No haneul config found in `~/.haneul/haneul_config/client.yaml`, create one [Y/n]?
```

You will see the following output:

```
Generated new keypair ...
  secret recovery phrase : [recovery phrase words are here]
Created "~/.haneul/haneul_config/client.yaml"
Set active environment to testnet
```

:::caution

Store recovery phrases securely and do not share them with anyone, as they provide access to any objects and tokens that an address owns.

It will not be visible again once the CLI history disappears.

[Learn more](/guides/developer/getting-started/get-address.mdx) about Haneul addresses, key generation, and recovery phrases.

:::

## `client.yaml`

Your Haneul client is now configured. By default, Haneul stores this information in either the `~/.haneul/haneul_config/client.yaml` file (macOS/Linux) or `%USERPROFILE%\.haneul\haneul_config\client.yaml` file (Windows). You can store a `client.yaml` file in a different location, if preferred, and specify its location with the `--client.config` flag.

The `client.yaml` contains the configuration for connecting to different Haneul networks (Testnet, Mainnet, Devnet, and Localnet), as well as your current active environment, which tells the CLI which network to use when you don't explicitly specify one.

You can modify your configuration using the `haneul client` subcommands. See the output of `haneul client --help` for more information.

Haneul stores the key for the Haneul address in a separate file, `~/.haneul/haneul_config/haneul.keystore` (macOS/Linux) or `%USERPROFILE/.haneul/haneul_config/haneul.keystore` (Windows). Learn more about Haneul addresses in [Create a Haneul Address](/guides/developer/getting-started/get-address.mdx).

<div className="next-steps-module">
  <div className="next-steps-header">
    <h3>Next steps</h3>
  </div>
  <div className="next-steps-grid">
    <Card className="plausible-event-name=client+address+button"
      title="Learn More About Haneul Addresses"
      href="/guides/developer/getting-started/get-address"
    >
      Now that you have created a Haneul address, learn about address management, key pairs, and recovery phrase best practices.
    </Card>
    <Card className="plausible-event-name=client+faucet+button"
      title="Get HANEUL from Faucet"
      href="/guides/developer/getting-started/get-coins"
    >
      Obtain HANEUL from a faucet to deploy packages on Testnet.
    </Card>
        <Card className="plausible-event-name=client+hello+button"
      title="Hello, World!"
      href="/guides/developer/getting-started/hello-world"
    >
      Clone the "Hello, World!" project.
    </Card>
  </div>
</div>
