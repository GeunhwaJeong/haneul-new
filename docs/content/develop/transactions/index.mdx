---
title: Transactions
description: Every update on Haneul, whether to the network itself or to objects on the network, happens through a transaction. Transactions handle everything from creating objects and minting assets to managing network operations.
keywords: [ ptbs, system transactions, programmable transaction, transaction blocks, transactions, haneul transactions, transaction metadata ]
pagination_prev: null
---

Every update on Haneul, whether to the network itself or to objects on the network, happens through a transaction. Transactions handle everything from creating objects and minting assets to managing network operations.

import DocCardList from '@theme/DocCardList';

<DocCardList />