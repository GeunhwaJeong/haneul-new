# Filtering on package and module
{
  transactionBlocks(
    filter: {
      function: "0x3::haneul_system"
    }
  ) {
    nodes {
      sender {
        address
      }
      gasInput {
        gasPrice
        gasBudget
      }
    }
  }
}
