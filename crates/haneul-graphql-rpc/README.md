# haneul-graphql-rpc

## Spinning up locally

See [src/commands.rs](src/commands.rs) for all CLI options.

```
cargo run --bin haneul-graphql-rpc start-server [--rpc-url] [--port] [--host] [--config]
```

This will build haneul-graphql-rpc and start an IDE:

```
Starting server...
Launch GraphiQL IDE at: http://127.0.0.1:8000
```
